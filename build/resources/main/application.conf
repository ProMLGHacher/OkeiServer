ktor {

    development = true

    deployment {
        host = "192.168.10.33"
        port = 3434
        port = ${?PORT}
	watch = [ classes ]
        responseWriteTimeoutSeconds = 10
    }
    application {
        modules = [ ru.krea.ApplicationKt.module ]
    }

    jwt {
            secret = "secret"
            issuer = "http://localhost:9090/"
            audience = "http://localhost:9090/all"
            realm = "Access to 'all'"
        }
}
